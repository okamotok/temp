require 'spec_helper'

describe "Finding movies by director" do
  subject {page}


  describe "Movie description page should have link to similar movies" do
    #movie = Movie.create!(name: "ABC")
    visit '/movies'
    it { should have_link('Find Similar Movies') }
  end

end # "Finding movies by director"

